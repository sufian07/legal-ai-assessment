# Runtime dependencies. Pin versions for reproducibility.
# See requirements-dev.txt for lint, typecheck, and test tooling.

# Core
anthropic==0.40.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Document processing (TASK-001)
pdfplumber==0.11.4
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.0.0

# Handwriting OCR (TASK-001 — TrOCR via HuggingFace)
# torch is large; install separately on GPU-less machines with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers==4.45.2
torch==2.5.0

# Structured extraction (TASK-002)
python-dateutil==2.9.0.post0

# Retrieval & embeddings (TASK-003)
sentence-transformers==3.2.1
chromadb==0.5.15
nltk==3.9.1

# Dashboard (TASK-006)
streamlit==1.40.0
streamlit-extras==0.5.0
streamlit-ace==0.1.1
plotly==5.24.1
